import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


const router = Router(); // eslint-disable-line no-undef


ReactDOM.render(<App router={router} />, document.querySelector('.App'));
